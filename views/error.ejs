<%- include('layout', { body: `
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8 text-center">
            <div class="error-page">
                <div class="error-animation mb-4">
                    <i class="bi bi-exclamation-octagon display-1 text-danger"></i>
                </div>
                
                <h1 class="display-4 fw-bold text-light mb-3">Oops!</h1>
                <h2 class="h4 text-muted mb-4">Terjadi Kesalahan</h2>
                
                <p class="text-muted mb-4">
                    Maaf, terjadi kesalahan pada server. Tim kami telah diberitahu tentang masalah ini 
                    dan sedang bekerja untuk memperbaikinya.
                </p>
                
                <% if (typeof error !== 'undefined' && error && process.env.NODE_ENV === 'development') { %>
                    <div class="alert alert-danger text-start mt-4">
                        <h6>Error Details (Development Mode):</h6>
                        <pre class="mb-0"><%= error.stack || error.message || error %></pre>
                    </div>
                <% } %>
                
                <div class="error-actions mt-4">
                    <a href="/" class="btn btn-primary btn-lg me-3">
                        <i class="bi bi-house me-2"></i>
                        Kembali ke Beranda
                    </a>
                    <button onclick="window.location.reload()" class="btn btn-outline-primary btn-lg">
                        <i class="bi bi-arrow-clockwise me-2"></i>
                        Coba Lagi
                    </button>
                </div>
                
                <div class="mt-5">
                    <h5 class="text-light mb-3">Jika masalah berlanjut:</h5>
                    <ul class="list-unstyled text-muted">
                        <li class="mb-2">
                            <i class="bi bi-check-circle text-success me-2"></i>
                            Periksa koneksi internet Anda
                        </li>
                        <li class="mb-2">
                            <i class="bi bi-check-circle text-success me-2"></i>
                            Coba refresh halaman beberapa saat lagi
                        </li>
                        <li class="mb-2">
                            <i class="bi bi-check-circle text-success me-2"></i>
                            Hubungi admin jika error terus terjadi
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.error-page {
    padding: 3rem 0;
}

.error-animation i {
    animation: shake 0.5s infinite alternate;
}

@keyframes shake {
    0% { transform: translateX(0); }
    100% { transform: translateX(5px); }
}

pre {
    font-size: 0.8rem;
    max-height: 200px;
    overflow-y: auto;
    background: rgba(0, 0, 0, 0.2);
    padding: 1rem;
    border-radius: 4px;
}

@media (max-width: 768px) {
    .error-page {
        padding: 2rem 0;
    }
    
    .error-actions .btn {
        display: block;
        width: 100%;
        margin-bottom: 1rem;
    }
    
    .error-actions .me-3 {
        margin-right: 0 !important;
    }
}
</style>
` }) %>

